apiVersion: apps/vl
kind: SET KIND
metadata:
  name: myapp-deployment
  labels:
    app: myapp
    type: front-end
spec:
  template:
    metadata:
      name: myapp-pod 
      labels:
        app: myapp
        type: front-end 
    spec:
      containers:
      - name: nginx-container
        image: ADD NGINX IMAGE
replicas: SET 4 REPLICAS
selector:
  matchLabels:
  type: front-end